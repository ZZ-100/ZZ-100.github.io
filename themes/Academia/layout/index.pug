extends includes/layout

block content
    .page(itemscope itemtype='https://schema.org/CreativeWork')
          each article in page.posts.data
              if article.academia
                  article(itemprop='articleBody')!= article.content
